package ${packageName}.domain.DTO;
#foreach ($import in $importList)
import ${import};
#end
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;

import java.util.ArrayList;
import java.util.List;
#if($table.crud || $table.sub)
#elseif($table.tree)
#end

/**
 * @Auther: ${author}
 * @Date: ${datetime}
 * @Description: ${functionName}返回对象 ${tableName}
 * @Version: 1.0.0
 */
#if($table.crud || $table.sub)
    #set($Entity="BaseVO")
#elseif($table.tree)
    #set($Entity="NodeVO")
#end
@ApiModel(description = "${functionName}返回对象 ${tableName}")
@Data
public class ${ClassName}VO extends ${Entity} {

#foreach ($column in $columns)
#if (!$table.isSuperColumn($column.javaField))
    @ApiModelProperty(value = "$column.columnComment")
#if($column.javaType == 'Date')
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
#end
#set($parentheseIndex=$column.columnComment.indexOf("（"))
#if($parentheseIndex != -1)
    #set($comment=$column.columnComment.substring(0, $parentheseIndex))
#else
    #set($comment=$column.columnComment)
#end
#if($parentheseIndex != -1)
    @Excel(name = "${comment}", readConverterExp = "$column.readConverterExp()")
#elseif($column.javaType == 'Date')
    @Excel(name = "${comment}", width = 30, dateFormat = "yyyy-MM-dd HH:mm:ss")
#else
    @Excel(name = "${comment}")
#end
    private $column.javaType $column.javaField;
#end
#end
#if($table.sub)

    @ApiModelProperty(value = "$subTable.functionName")
    private List<${subClassName}VO> ${subclassName}VOs = new ArrayList<>();
#end

}

