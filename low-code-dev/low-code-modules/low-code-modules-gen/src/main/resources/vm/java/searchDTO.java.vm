package ${packageName}.domain.DTO;
#foreach ($import in $importList)
import ${import};
#end
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;
#if($table.crud || $table.sub)
#elseif($table.tree)
import java.util.Date;
#end

/**
 * @Auther: ${author}
 * @Date: ${datetime}
 * @Description: ${functionName}查询对象 ${tableName}
 * @Version: 1.0.0
 */
#if($table.crud || $table.sub)
    #set($Entity="extends PageDTO")
#elseif($table.tree)
    #set($Entity="implements Serializable")
#end
@Data
@ApiModel(description = "${functionName}查询对象 ${tableName}")
public class ${ClassName}SearchDTO ${Entity} {

#foreach ($column in $columns)
#if (!$table.isSuperColumn($column.javaField))
#if($column.query)
    @ApiModelProperty(value = "$column.columnComment")
#if($column.javaType == 'Date')
#if($column.queryType == 'BETWEEN')
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
    private Date startTime;

    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
    private Date endTime;
#else
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
    private $column.javaType $column.javaField;
#end
#else
    private $column.javaType $column.javaField;
#end
#end
#end
#end

}

