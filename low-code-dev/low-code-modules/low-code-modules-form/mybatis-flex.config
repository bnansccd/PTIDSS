# 开启 Mapper 自动生成
processor.mapper.generateEnable=true
# 开启 @Mapper 注解
processor.mapper.annotation=true
# 自定义 Mapper 生成的包名
processor.mapper.package=com.troy.form.mapper
# 自定义 Mapper 的父类
processor.mapper.baseClass=com.troy.common.datasource.mapper.MyBaseMapper
