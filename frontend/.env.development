# 开发环境：同源 /ptidss 由 vite 代理转发到本地后端 9080（浏览器仅访问前端 5173 单端口）
# V1.6.4 起：后端各域（market/trade/decision/settlement/report/ocr/intel）均已实现，
# 联调走 vite proxy（vite.config.ts server.proxy /ptidss → 9080）；Prism Mock（4010）仅用于契约演示
VITE_API_BASE=/ptidss
