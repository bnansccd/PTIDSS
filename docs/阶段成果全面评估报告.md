# 阶段成果全面评估报告（多角色交叉审查）

**评估范围**：DDL v1.0 锁定 + 数据字典 V1.1｜OpenAPI V1.0 契约 + Mock Server｜Vue3 前端骨架 + 联调冒烟｜多省 region 方案（V1.0→V1.1 会签）
**评估组织**：产品经理/系统架构师/高级开发工程师/测试工程师/数据管理员五角色
**评估时间**：2026-08-08　**关联**：开发基线 V1.6（5.7 交付物）｜SRS V1.1（9.2 评审决议）

---

## 一、评估结论

各阶段成果**整体达标**（DDL 静态校验通过、契约 lint 0 error、前端类型检查 0 error、Mock 联调 12 端点全 200），但多角色交叉审查发现 **7 项遗漏/偏差**（含 1 项此前联调阶段的**误判**，已修复）、登记 **2 项流程级待办**（契约 v1.1 缺口清单、前端页面迭代路线）；**DDL v1.0.1 勘误批次（附录 A，5 项）已由数据组走 7.5 变更流程执行闭环**。详见第四/五节。

## 二、逐项成果评估（五角色）

| 成果 | 产品 | 架构 | 开发 | 测试 | 数据 | 结论 |
|---|---|---|---|---|---|---|
| DDL v1.0（51 表 + 8 超级表 + 2 视图） | ✓ 覆盖评审决议五项 | ✓ 分区/索引合理 | ✓ 可实施 | ✓ 静态校验通过 | ✅ 勘误已执行（v1.0.1，附录 A） | 锁定有效（v1.0.1 勘误版） |
| 数据字典 V1.1 | ✓ | ✓ | ✓ | ✓ | ✓ 与 DDL 双向勾稽 | 一致 |
| OpenAPI V1.0 + Mock | ✓ 62 path 覆盖主要流程 | ✓ 统一响应/鉴权 | ✓ 可联调 | ✓ lint 0 error | ⚠ message 枚举缺 approval_task | 契约 v1.1 修正 |
| 前端骨架 + 冒烟 | ✓ 8 核心页 | ✓ 分层清晰 | ✓ 类型 0 error | ✓ 12 端点 200 | — | 达标（含本轮修复） |
| 多省方案 V1.1 | ✓ 省间归属定案 | ✓ 全链路透传 | ✓ 机制保障 | ✓ 测试策略补齐 | ✓ RLS 定案 | 会签通过 |

## 三、覆盖率核查（四层对照）

| 层 | 总量 | 已覆盖 | 覆盖率 | 说明 |
|---|---|---|---|---|
| SRS 功能需求（FR） | 38 项 | 契约 62 path 全覆盖 | ~100% | 逐 FR 追溯均有对应端点（抽查 12 项） |
| OpenAPI path | 62 | 前端 API 封装 24 | 39% | 骨架按核心流程接；其余登记迭代（第五节） |
| 4.7 页面清单 | 60+ 页 | 前端 8 页 | ~13% | 骨架定位合理；页面迭代路线见第五节 |
| DDL 表 | 51 业务表 + 8 超级表 | 数据字典全量勾稽 | 100% | 评审记录逐域勾稽通过 |

## 四、问题清单与处置（E1–E7）

| # | 问题 | 发现角色 | 级别 | 处置 | 状态 |
|---|---|---|---|---|---|
| E1 | **/message/list 契约存在但联调阶段误判缺失**（根因：端点清单 grep 域范围漏了 message/flow/model 等 7 域），导致 getMessages 被误删、驾驶舱待办降级为占位 | 测试/开发 | 高 | 恢复 api/message.ts（getMessages + markRead）、DashboardView 待办列表（含 approval_task 兼容展示）、Mock 冒烟 200 | ✅ 已修复 |
| E2 | **契约 /message/list msgType 枚举缺 approval_task**（DDL message_record CHECK 已有），评审决议④审批消息契约不可表达 | 数据/测试 | 中 | 前端兼容展示；契约 v1.1 修正（第五节清单 #3） | ✅ 前端修复/契约登记 |
| E3 | **前端切区缺陷**：区域切换后路由不变、页面 onMounted 不重跑，数据仍为旧区；MainLayout periodMode 为非响应式常量，切区不更新 | 开发 | 高 | MainLayout：periodMode 改 computed + 切区整页重载（注释登记正式实现路径）；E2E 用例入方案第十章 | ✅ 已修复 |
| E4 | **audit_log 无 region_code**：审计日志无法按省检索（等保审计诉求） | 架构 | 中 | DDL v1.0.1 勘误申请清单（附录 A 项 3） | ✅ 已随 v1.0.1 勘误执行（audit_log.region_code + ix_audit_log_region） |
| E5 | **Doris 数仓层隔离缺失**：方案 V1.0 未覆盖基线数据架构的 Doris 预聚合层 | 架构 | 中 | 方案 V1.1 新增 5.3（聚合键含 region_code、全国走 global 表） | ✅ 已修复 |
| E6 | **服务间调用 region 透传缺失**：8+ 微服务链路未定义头传递 | 架构 | 中 | 方案 V1.1 新增 4.5（Feign 拦截器 + fail-fast + 事件头） | ✅ 已修复 |
| E7 | **st_spot_price.region_code 可空**：现货行情存在无省数据风险 | 数据 | 低 | 质量规则种子补充登记（第五节 #5） | ✅ 已随 v1.0.1 勘误执行（质量规则种子 2 条） |

## 五、补充完善建议（登记项）

### 5.1 契约 v1.1 缺口清单（建议修改项）

| # | 项 | 说明 |
|---|---|---|
| 1 | X-Region-Code header 参数 | components/parameters 统一定义，业务 path 引用（方案 4.2 模板） |
| 2 | CurrentUser.regions | 用户授权区域列表（sys_user_region 契约化） |
| 3 | /message/list msgType 枚举补 approval_task | 与 DDL CHECK 对齐（评审决议④） |
| 4 | /decision/sessions、/optimize/joint-tasks、/optimize/backtests 补列表 GET | 决策页历史列表（当前骨架为发起型演示） |
| 5 | /ocr/tasks 补 GET 列表 | OCR 识别历史（当前仅 POST） |

### 5.2 前端页面迭代路线（4.7 清单 → 骨架 8 页 → 迭代）

| 批次 | 页面 | 依赖契约 |
|---|---|---|
| 第一批（已接） | 登录/驾驶舱/行情/交易/决策/结算/情报/系统管理 | V1.0 |
| 第二批（建议） | 消息中心 /messages、持仓 /trade/positions、成交结果 /trade/results、策略库 /optimize/strategies、复盘 /review/dashboard | V1.0 已具备，纯前端工作量 |
| 第三批（建议） | 政策库/解析/研判、数据源管理、模型管理、审批流、考核 | V1.0 已具备 + 契约 v1.1 补列表 GET |

### 5.3 质量规则种子补充（数据组）

- st_spot_price / st_midlong_price 的 region 非空质量规则（E7 联动，**已随 v1.0.1 勘误批次执行**：07_seed_data.sql 种子 DQ-TS-REGION-NN-SPOT / DQ-TS-REGION-NN-MIDLONG，2026-08-08）
- sys_region.status 与 market_support 匹配校验（disabled 省不得配现货市场）

## 六、评估遗留确认（评审记录 05 遗留项状态）

| 遗留项 | 状态 |
|---|---|
| 1. ocr_task region_code | 保持经 settlement_record 回溯，多省量级验证后决策（会签确认） |
| 2. 分区表月度分区预建 | 数据组按 pYYYYMM 规范管理（不阻塞） |
| 3. RLS 启用决策 | **已随方案会签定案并复审调整**（首期启用 RLS + 按机构过滤 + 业务数据按机构分库；2026-08-08 复审，方案 V1.2 5.1/5.8） |

## 附录 A：DDL v1.0.1 勘误申请清单（数据组走 7.5 变更流程）

> **已另附独立执行文件**：`docs/ddl/08_勘误申请清单_v1.0.1.md`（申请编号 DDL-ERR-2026-001，含影响面/审批流/执行回归清单），由数据组按开发基线 7.5 变更管理流程执行；下表为摘要。**2026-08-08 复审后批次扩为 5 项**（新增 RLS 策略脚本）。**执行状态：☑ 全部执行并回归（2026-08-08，见 08 清单七章执行记录与 05 评审记录七章登记）**。

| # | 对象 | 勘误内容 | 依据 |
|---|---|---|---|
| 1 | 02_tdengine_schema.sql st_spot_price | region_code 由普通列提升为 TAG（子表划分含 region 维度，子表必填、天然非空） | **业务数据按机构分库定案（方案 V1.2 5.2/5.8 复审）：必须执行项**；未实施期变更成本最低 |
| 2 | 02_tdengine_schema.sql st_midlong_price | 同上（保留 market_center 列） | 同上 |
| 3 | 01_postgres_schema.sql audit_log | 补 region_code VARCHAR(16)（可空，操作归属区域）+ 索引 | E4（等保审计按省检索）+ 多租户/审计诉求自始成立（方案 V1.2 5.1） |
| 4 | 07_seed_data.sql | 补充行情 region 非空质量规则种子 | E7 / 5.3 |
| 5 | **新增 09_rls_policies.sql** | **RLS 首期启用脚本**：13 表 ENABLE RLS + 策略 USING region_code = current_setting（intel_news 含全国可见规则）+ 每服务独立账号授权 | 方案 V1.2 5.1 复审定案（RLS 首期启用） |
