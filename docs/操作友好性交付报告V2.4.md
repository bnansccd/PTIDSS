# PTIDSS 操作友好性交付报告 V2.4

- **批次**：task-31e（V2.3 延续）
- **日期**：2026-08-22
- **任务**：针对客户 **11 项操作友好性诉求**的落地——① 成效考核指标体系可新增/编辑/自定义；② 报表模板可编辑自定义；③ 报表实例按模板+时间周期生成；④ 全系统日期时间可选输入（选择即收起、周期类+对应周期）；⑤ 审批流发起前选业务单号（选类型引已有单号或自动生成）；⑥ 申报单生成/编辑完善、关联业务、交易系统接口配置与状态监测；⑦ 算法注册表支持 jar/zip/json 等格式上传解析适配；⑧ 模型平台训练/评估/推理出具详细报告与对标变化；⑨ 系统英文/简缩术语全局"编码+名称"显示；⑩ 排查修复"点击刷新即退出系统"（令牌滑动续期）；⑪ 市场化交易主要省份调研、数据源/情报源接入、长期验证方案。
- **需求基线**：客户 11 项诉求（本报告 §2 逐条对照）

---

## 1. 结论（一句话）

**V2.4 完成操作友好性 11 项全量落地**：成效考核指标库与报表模板实现"可新增/可编辑/可自定义"完整 CRUD；报表实例支持"选模板 + 时间周期"一键生成并导出；全系统时间日期输入统一为 `datetime-local` 且选择即收起，周期类时间自动"+周期"；审批流发起按业务类型引入已有单号或自动生成有效单号；申报单补全生成/编辑/网关推送配置与状态监测；算法注册表支持 jar/zip/json 等文件深度解析适配；模型任务（训练/离线评估/在线推理）出具过程步骤 + 结果指标 + 与上次对标（delta/变化摘要）完整报告；系统业务侧术语全面"中文名（编码）"显示；修复"刷新即退出"根因（JWT 固定过期不续期）为令牌滑动续期；完成 7 家正式运行省级现货市场调研并接入 6 省区域 + 10 个数据源/情报源 + 长期验证方案。回归验证 **305/305 用例全绿** + 新增 API 16/16，后端编译 0 错误、前端 vue-tsc + vite build 通过，交付态 captcha=true 已恢复。

## 2. 客户 11 项诉求 → 落地对照

| # | 诉求（客户原话） | 落地 | 验证 |
|---|---|---|---|
| 1 | 成效考核-考核指标体系，可新增、编辑、自定义 | AssessmentController 新增指标 CRUD：`GET /assessment/indicators`（分页）、`GET /indicators/all`（全量）、`POST /indicators`、`PUT /indicators/{id}`；指标含编码/名称/口径/权重/周期/启停自定义字段；AssessView 指标管理页签新增/编辑弹窗 + 启停切换 | ✅ 接口实测 200；前端 vue-tsc/build 通过 |
| 2 | 报表中心-报表模板，要求可编辑和自定义 | ReportController：`GET /report/templates`（分页）、`GET /templates/all`、`POST /templates`、`PUT /templates/{id}`；模板含名称/周期类型/指标集/布局/表头口径 JSON 自定义；ReportView 模板列表新增/编辑弹窗 + 启停 | ✅ 接口实测 200；前端通过 |
| 3 | 报表中心-生成报表实例，可选择报表模板和时间周期，生成报表实例 | ReportController：`POST /report/instances`（templateId + periodType + periodValue → 生成实例，周期联动：日=日期、月=年月、年=年份）、`GET /report/instances`、`GET /instances/{id}/export`（CSV 报送格式：表头+口径说明+指标行）；ReportView 实例页签"选模板 → 选周期 → 生成"联动表单 + 导出下载 | ✅ 接口实测生成/导出 200；前端通过 |
| 4 | 全系统日期时间可选输入，选择后日期时间输入选框控件自动收起；周期类时间选择时间后+对应周期 | 全部时间日期输入统一 `datetime-local` 选择器（V2.3 已铺开），本期补齐选择后自动收起（change 事件失焦）；周期类时间（报表实例/任务周期）选择后自动按周期类型换算对应周期值 | ✅ 前端实测收起 + 周期联动；后端宽松解析回归通过 |
| 5 | 审批流-发起流程前要求选业务单号，选类型后引入该类型已有单号或自动生成有效单号 | FlowController：`GET /flow/biz-types`（业务类型清单）、`GET /flow/biz-options?bizType=`（该类型已有单号，仅传业务类型时返回单号 + 自动生成规则）；`POST /flow/start` 校验 bizId 必填且属于所选类型；FlowView 发起流程弹窗"选类型 → 引已有单号（datalist 可搜索）或自动生成" | ✅ 接口实测 200；发起流程携带单号通过 |
| 6 | 交易申报-申报单：只有查询，需生成及编辑补充完善，关联优化业务，交易系统接口配置和状态监测 | TradeController：`POST /trade/declarations`（创建：tradeDate/marketType/stage/items 明细）、`PUT /trade/declarations/{id}`（编辑补充：合规校验/文件/明细）、`POST /declarations/{id}/submit`（提交→网关推送）；gateway 配置表（DDL 12 trade_gateway_config）+ declaration 表新增 gateway_push_status/time/detail 三列（提交后状态监测 pending/success/failed/skipped + 回执信息）；TradeView 申报页签"新增/编辑/提交/状态监测"完整操作闭环 | ✅ 回归 39/39（创建→列表→提交→持仓/成交）；本次补执行 DDL 12 三列 ALTER 后列表回归通过 |
| 7 | 模型平台-算法注册表，客户可自定义上传算法 jar、zip、json 等各种格式，能解析和适配系统应用 | AlgorithmService 深度解析扩展：json 合法对象提取 paramsSchema；zip 遍历成员清单、识别 manifest/README/参数文件；jar 解析 META-INF/MANIFEST.MF + 类名扫描；按内容关键字猜测类目（risk/cvar→risk_measure、quote/offer→quote_strategy、settle→settlement、review→review、optimize/milp→optimize、rule/drools→rule_engine、market/senti→market_analysis、缺省 forecast）+ 生成说明/版本/编码；前端 ModelView 算法弹窗"选择算法文件 → 自动解析回填" | ✅ V2.3 起算法解析 2 用例 + 本期 jar/zip 深度解析实测通过 |
| 8 | 对模型平台，训练触发/离线评估/在线推理等，出具详细报告和过程，与前面对标及变化 | 新增 model_task 表（DDL 13：task_type CHECK train/evaluate/inference、input_json/process_steps/result_json/compare_json）；ModelService.runTask：类型校验→插 running→doTrain/doEvaluate/doInference 逐步记录 process_steps（步骤+耗时）→成功落 result_json/latencyMs → 查同模型同类型上次 success 任务计算 compare_json（baselineTaskId/baselineMetrics/delta/summary，delta 对 mape/rmse/directionAccuracy/confidence/avg/peak/trough 逐项差值）；ModelController：`POST /model/tasks/{taskType}`、`GET /model/tasks`、`GET /model/tasks/{id}`；ModelView 训练/评估/推理卡片展示报告摘要 + 对标 badge + 完整报告弹窗（过程步骤表 + 指标表 + 对标 delta 表，好坏方向绿色标注） | ✅ 运行时验证：train 4 步骤+结果；evaluate mape 0.0729/directionAccuracy 0.878/passed；inference 4 步骤+confidence 0.88；第二次 train 出现 compare（delta/summary） |
| 9 | 系统全面对英文或简缩术语，在应用业务侧实现编码+名称显示 | 11 个业务视图补状态/类型标签函数，展示格式统一"中文名（编码）"：SettlementView（同步状态/差异类型/状态）、PolicyView（发布状态）、ReviewView（报告状态）、ForecastView（任务/模型状态）、AgentView（运行状态）、IntelView（重要度/情报类型/采集方式/连接方式/规则状态）、OptimizeView（任务状态）、DashboardView（状态/场景/计划类型）、DataManageView（模板类型/周期/任务/复核状态）、AdminView（启停状态）、FlowView（待办状态） | ✅ vue-tsc + vite build 通过；各视图运行实测正常 |
| 10 | 很多生成或查询界面涉及刷新操作，排查"点击刷新即退出系统" | **根因**：JWT exp 固定 120 分钟，Caffeine 会话缓存续期但 JWT 过期后 getUserKey 返回 null → 前端登出。**修复（令牌滑动续期）**：TokenService 抽取 buildClaims，verifyToken 剩余不足阈值（默认 10min）时刷新缓存 expireTime + 重签 JWT 写 LoginUser.newToken（@JsonIgnore）；HeaderInterceptor 写响应头 `X-New-Token`；前端 http.ts 响应拦截器读 x-new-token → useAuthStore().setToken() 更新 localStorage；新增配置 `ptidss.token.expire-minutes` / `ptidss.token.refresh-threshold-minutes` | ✅ 2min/1min 参数实测：登录后立即请求无新令牌（符合预期）；等待 80s 后请求返回 X-New-Token，新令牌请求 200 → 滑动续期闭环成功 |
| 11 | 搜索当前实施市场化交易的主要地区和省份，对接数据源和情报源，接入数据，长时间验证可靠性 | **调研**：2025 年底全国正式运行省级现货市场 7 家（山西/广东/山东/甘肃 + 蒙西/湖北/浙江），19 省试运行，南方区域市场连续运行。**接入**：DDL 14 注册 6 省区域（CN-37 山东/CN-14 山西/CN-44 广东/CN-62 甘肃/CN-15 蒙西/CN-42 湖北，market_support/exchange_channel/settlement_period 按省配置）+ 10 个数据源/情报源（各省现货出清价格 api 15 分钟、山东公告/供需披露、蒙西供需披露、各省规则公告 crawl、南方区域跨区出清价格）；接口实测返回。**长期验证**：docs/V2.4市场化省份接入与长期验证方案.md（指标：采集成功率≥98%/时效≥95%/数据完整性≥99%/异常率≤1%/模型 MAPE≤8%/方向准确率≥80%/算法成功率≥99%；周期：日/周/月/季；方法：复用任务报告对标 + 数据质量规则 + 调度监控） | ✅ DDL 执行 16 行插入；/admin/regions 6 省返回、/intel/sources 10 源返回 |

## 3. 实施清单

### 3.1 数据库（DDL 3 个）

| 文件 | 说明 |
|---|---|
| docs/ddl/12_trade_gateway_config.sql | 交易网关配置表（endpoint/auth 等）+ declaration 表 gateway_push_status/time/detail 三列（本批补执行 ALTER） |
| docs/ddl/13_model_task.sql | model_task 表：task_type CHECK + input_json/process_steps/result_json/compare_json JSONB |
| docs/ddl/14_market_regions_v2_4.sql | 6 省市场化区域 + 10 个数据源/情报源（WHERE NOT EXISTS 幂等） |

### 3.2 后端（核心文件）

| 文件 | 说明 |
|---|---|
| review/controller/AssessmentController.java | 考核指标 CRUD（indicators 分页/全量/新增/编辑） |
| report/controller/ReportController.java | 报表模板 CRUD + 实例生成（模板+周期联动）+ CSV 导出 |
| flow/controller/FlowController.java | 发起流程 biz-types/biz-options 业务单号选项 + start 校验 |
| trade/controller/TradeController.java + trade/domain/Declaration.java | 申报单创建/编辑/提交 + 网关推送状态字段 |
| model/service/AlgorithmService.java | 算法文件深度解析（json/zip/jar 包解析 + 类目猜测 + 参数模板 + 说明生成） |
| model/service/ModelService.java + model/controller/ModelController.java | runTask 三类型任务 + processSteps/resultJson/compareJson 报告 + 3 接口 |
| common/security/TokenService.java、HeaderInterceptor.java、LoginUser.java、common/constant/Constants.java | 令牌滑动续期（重签 JWT + X-New-Token 响应头） |
| resources/application.yml | `ptidss.token.expire-minutes:120`、`ptidss.token.refresh-threshold-minutes:10` |

### 3.3 前端

| 文件 | 说明 |
|---|---|
| views/AssessView.vue | 指标体系新增/编辑弹窗 + 启停 |
| views/ReportView.vue | 模板编辑弹窗 + 实例生成（选模板+周期联动）+ 导出 |
| views/FlowView.vue | 发起流程选业务类型 → 引已有单号/自动生成 |
| views/TradeView.vue | 申报单新增/编辑/提交 + 网关推送状态监测 |
| views/ModelView.vue | 算法文件解析回填 + 模型任务报告卡片（过程步骤/指标/对标弹窗） |
| 11 个视图（Settlement/Policy/Review/Forecast/Agent/Intel/Optimize/Dashboard/DataManage/Admin/Flow） | 编码+名称标签函数（中文名（编码）） |
| api/model.ts、api/types.ts、api/http.ts | 任务报告 3 函数、Declaration 网关字段、x-new-token 响应拦截更新令牌 |

### 3.4 测试

- 回归基线：verify_platform 117 / verify_full_user 94 / verify_agent 50 / verify_v2_audit 33 / verify_v23_ux 11 = **305/305**
- 新增 API：verify_new_api 16/16（含需求 5 单号选项、需求 8 任务报告等）
- 需求 10 专项：2min/1min 短参数实测滑动续期闭环

## 4. 回归验证结果

| 项 | 结果 |
|---|---|
| `mvn compile`（JDK8 离线） | ✅ 0 错误 |
| `vue-tsc --noEmit` | ✅ 0 错误 |
| `vite build` | ✅ 全部 chunk 构建成功 |
| `verify_platform.py`（117 用例） | ✅ 117/117 |
| `verify_full_user.py`（94 用例） | ✅ 94/94（首跑 92/94，2 项 500 为 declaration 表缺 gateway 三列，补执行 DDL 12 ALTER 后全过） |
| `verify_agent.py`（50 用例） | ✅ 50/50 |
| `verify_v2_audit.py`（33 用例） | ✅ 33/33 |
| `verify_v23_ux.py`（11 用例） | ✅ 11/11 |
| `verify_new_api.py`（16 用例） | ✅ 16/16 |
| **合计** | ✅ **321/321 PASS** |
| 交付态 | ✅ 后端 9080 正式配置运行（captcha=true，expire=120min） |

## 5. 遗留建议

| 项 | 说明 | 建议 |
|---|---|---|
| 各省行情接口 | 数据源台账已就位，实际行情抓取需生产环境配置交易中心公开接口/公告页地址 | 按《V2.4市场化省份接入与长期验证方案》接入并配置重试/降级 |
| 长期验证执行 | 90 天持续观测为上线后动作 | 上线后按方案日/周/月/季节奏执行并留档 |
| 算法包规范 | jar/zip 二进制包解析为启发式 | 可扩展"算法包规范"（jar 内 manifest/同包 params.json）实现全自动适配 |
| 新令牌丢失场景 | X-New-Token 在无响应头（如 401/网络异常）时不更新本地令牌 | 前端可在 401 时联动 re-login 提示，避免极端场景登出 |

## 6. 交付物清单

- 后端：AssessmentController（指标 CRUD）、ReportController（模板 CRUD + 实例生成/导出）、FlowController（biz-types/biz-options）、TradeController + Declaration（申报生成/编辑/提交/网关状态）、AlgorithmService（深度解析）、ModelService/ModelController（任务报告 3 接口）、TokenService/HeaderInterceptor/LoginUser/Constants（滑动续期）、application.yml（token 配置）
- 前端：AssessView/ReportView/FlowView/TradeView/ModelView 改造 + 11 视图标签函数 + api/model.ts/types.ts/http.ts
- 数据库：docs/ddl/12_trade_gateway_config.sql、13_model_task.sql、14_market_regions_v2_4.sql（均已执行）
- 测试：回归 305/305 + 新增 API 16/16
- 文档：docs/V2.4市场化省份接入与长期验证方案.md + 本报告
